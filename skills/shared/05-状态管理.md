# 状态管理（共用）

- **库**：Pinia（适用于应用；库包由使用方管理状态）。
- **模块划分**：按业务域建 store 文件（如 `stores/user.ts`、`stores/app.ts`），避免单文件过大。
- **与请求配合**：接口数据如需跨页面共享再放入 store；仅单页使用的放在页面或 composable 中。
- **命名**：store id 与文件名一致，清晰表达用途。

单项目/联邦项目/联邦库下的作用域与使用方式见各模式 05-状态管理。
