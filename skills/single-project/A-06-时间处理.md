# A-06 时间处理（单项目）

单项目下所有与日期相关的逻辑均在**本应用**内，统一使用 dayjs。

- **统一使用 dayjs**：所有日期格式化、解析、计算均用 dayjs，禁止手写日期格式化/解析逻辑。
- **按需引入**：如 `import dayjs from 'dayjs'`，需插件时单独引入（如 `customParseFormat`、`utc`）。
- **依赖**：在根 `package.json` 安装 dayjs 即可，无多包共享问题。

与 B/C 共用规则一致，见 [shared/06-时间处理.md](../shared/06-时间处理.md)。
