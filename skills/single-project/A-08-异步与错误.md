# A-08 异步与错误（单项目）

单项目下仅一套请求与一套拦截器，公共错误与业务错误的边界在本应用内统一。

- **写法**：异步逻辑统一使用 `try/catch`，并给出明确兜底（默认值、提示、降级行为）。
- **公共错误**：在**本应用**的 HTTP/请求拦截器中统一处理（如 401 跳转登录、5xx 提示、网络错误提示）。
- **业务错误**：在页面或 composable 中就近处理（如接口返回 code !== 200 时的提示或分支逻辑）。

与 B/C 共用规则一致，见 [shared/08-异步与错误.md](../shared/08-异步与错误.md)。
