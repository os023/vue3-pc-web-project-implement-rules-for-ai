# A-07 数组、对象与判断（单项目）

单项目下数组、对象操作及常用判断统一使用 lodash-es，在本应用内按需引入。

- **优先使用 lodash-es**：避免重复造轮子；从 `lodash-es` 按函数引入，减少打包体积。
- **示例**：`import { get, debounce } from 'lodash-es'`。
- **依赖**：在根 `package.json` 安装 lodash-es 即可。

与 B/C 共用规则一致，见 [shared/07-数组对象判断.md](../shared/07-数组对象判断.md)。
